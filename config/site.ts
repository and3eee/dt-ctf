export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "SUP CTF",
	description: "Time to capture a flag",
	navItems: [
		{
			label: "Home",
			href: "/",
		},
	],
	navMenuItems: [
		
	],
	links: {
	
	},
};
